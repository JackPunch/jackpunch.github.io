# Hugo 生成的静态文件（不需要提交，每次构建会重新生成）
/public/

# Hugo 缓存的资源文件（如处理后的图片、CSS/JS 压缩文件）
/resources/_gen/

# Hugo 构建锁文件（防止并发构建冲突，本地临时文件）
.hugo_build.lock

# 操作系统生成的临时文件
.DS_Store
Thumbs.db

# 如果你用 npm/yarn 管理前端依赖（可选）
node_modules/
package-lock.json
yarn.lock

# 如果你有自定义的构建脚本或临时文件（可选）
tmp/
*.log
